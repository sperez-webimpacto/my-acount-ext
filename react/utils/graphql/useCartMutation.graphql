mutation useCartMutation(
  $orderFormId: String
  $items: [CartUseInfo]
  $customData: CustomAppGiftCardInput
  $userType: String
) {
  useCart(
    orderFormId: $orderFormId
    items: $items
    customData: $customData
    userType: $userType
  )
}
